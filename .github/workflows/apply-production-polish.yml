name: Apply production polish

on:
  push:
    paths:
      - 'assets/production-polish.css'
      - 'assets/favicon.svg'
      - '.github/workflows/apply-production-polish.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Inject production assets
        run: |
          python3 - <<'PY'
          from pathlib import Path

          css_marker = '<link rel="stylesheet" href="assets/production-polish.css">'
          favicon_marker = '<link rel="icon" type="image/svg+xml" href="assets/favicon.svg">'
          changed = []
          for path in sorted(Path('.').glob('*.html')):
              text = path.read_text(encoding='utf-8')
              additions = []
              if css_marker not in text:
                  additions.append(css_marker)
              if favicon_marker not in text:
                  additions.append(favicon_marker)
              if not additions:
                  continue
              if '</head>' not in text:
                  raise SystemExit(f'Missing </head> in {path}')
              text = text.replace('</head>', '\n'.join(additions) + '\n</head>', 1)
              path.write_text(text, encoding='utf-8')
              changed.append(str(path))
          print('Updated:', ', '.join(changed) if changed else 'nothing')
          PY

      - name: Verify site structure
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          html_files = sorted(Path('.').glob('*.html'))
          css_marker = '<link rel="stylesheet" href="assets/production-polish.css">'
          favicon_marker = '<link rel="icon" type="image/svg+xml" href="assets/favicon.svg">'
          failures = []
          if not Path('assets/favicon.svg').exists():
              failures.append('assets/favicon.svg missing')
          for path in html_files:
              text = path.read_text(encoding='utf-8')
              if css_marker not in text:
                  failures.append(f'{path}: polish stylesheet missing')
              if favicon_marker not in text:
                  failures.append(f'{path}: favicon link missing')
              if text.count('<h1') != 1:
                  failures.append(f'{path}: expected 1 H1, found {text.count("<h1")}')
              if not re.search(r'<meta[^>]+name=["\']description["\']', text, re.I):
                  failures.append(f'{path}: meta description missing')
              if not re.search(r'<link[^>]+rel=["\']canonical["\']', text, re.I):
                  failures.append(f'{path}: canonical missing')
          if failures:
              print('\n'.join(failures))
              raise SystemExit(1)
          print(f'PASS: {len(html_files)} HTML pages include polish CSS, favicon, one H1, meta description and canonical.')
          PY

      - name: Commit injected links
        run: |
          if git diff --quiet; then
            echo 'No HTML changes required.'
            exit 0
          fi
          git config user.name 'hellojoesolutions'
          git config user.email 'joe@hellojoe.solutions'
          git add '*.html'
          git commit -m 'Add production favicon across site pages'
          git push
